version: "3.9"
services:
  chanchito:
    build: .
    ports:
      - "9000:9000"
    links:
      - monguito
  monguito:
    image: mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=bimintatel
      - MONGO_INITDB_ROOT_PASSWORD=benraq
    volumes:
      - mongo-data:/data/db
      # mysql -> /var/lib/mysql
      # postgres -> /var/lib/postgresql/data

volumes:
  mongo-data:

